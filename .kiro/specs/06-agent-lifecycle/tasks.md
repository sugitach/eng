# 06-agent-lifecycle: Implementation Tasks

- [x] 1.1 `eng-agent` の引数処理 (`clap`) に `--daemon` などを追加
- [x] 1.2 Agentの二重起動チェックとプロセス間通信（既存Agentのポート特定）
- [x] 1.3 `Standard` モードにおける「全UI終了検知 -> Agent終了」ロジック実装
- [x] 1.4 `Daemon` モードにおけるバックグラウンド化（または制御返却）
- [x] 1.5 終了時のクリーンアップ処理（CoreへのShutdown信号送信）
- [x] 1.6 マルチUI起動の検証（既存Agentへの接続と新規ウィンドウ起動）
- [ ] 1.6 マルチUI起動の検証（既存Agentへの接続と新規ウィンドウ起動）
